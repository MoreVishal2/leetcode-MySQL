select m.employee_id employee_id,m.name name,count(e.employee_id) reports_count , round(avg(e.age)) average_age from Employees m inner join Employees e on e.reports_to=m.employee_id group by m.employee_id order by employee_id;